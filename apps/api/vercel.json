{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "// notes": [
    "All requests funnel through /api/index.ts (the NestJS serverless wrapper).",
    "The /health rewrite is explicit so uptime checks don't go through the same path matcher.",
    "ffmpeg-static + @resvg/resvg-js + chrome-launcher get pruned from the function bundle — they're only used by the lesson-generation CLI, never on HTTP requests."
  ],

  "functions": {
    "api/index.ts": {
      "maxDuration": 90,
      "memory": 1024,
      "excludeFiles": "**/{ffmpeg-static,resvg-js,chrome-aws-lambda,puppeteer*}/**"
    }
  },

  "rewrites": [
    { "source": "/health", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
